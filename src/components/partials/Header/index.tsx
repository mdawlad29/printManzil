import React from "react";

const Header = () => {
  return (
    <header className="py-3 bg-blue-500 text-white text-center text-lg font-semibold fixed top-0 w-full z-50">
      Header
    </header>
  );
};

export default Header;
